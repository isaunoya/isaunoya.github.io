将点分治推广为每次删除至多 $k + 1$ 个点。对于当前连通子图，任取生成树 $T$ 及其重心 $c$。连接 $T-c$ 不同分量的每条边都与树上路径组成一个经过 $c$ 的简单环，且这些环互不相同，因此这样的边至多有 $k$ 条。

将 $c$ 和每条跨分量边的任意一个端点加入 $S$，则 $abs(S) <= k + 1$。删除 $S$ 后，每个连通分量都包含于 $T-c$ 的某个分量中，大小至多减半，故递归深度为 $O(log n)$。

对每个分治结点的$s in S$，在当前子图中 BFS，预处理距离$d_(s)(v)$。维护$b_s$，表示$s$到当前子图中最近标记点的距离，初始为$infinity$。

标记$x$时，沿分治祖先用$d_(s)(x)$更新$b_s$；查询$v$时，沿分治祖先取

$ min_s (d_(s)(v) + b_s). $

候选值不会小于真实距离。取从 $v$ 到最近标记点的最短路，在分治过程中第一次与它相交的分隔集中，必有一个 $s$ 取到等号。

预处理时间为 $O((k + 1)(n + m) log n)$，空间为 $O((k + 1)n log n + m)$，单次操作时间为 $O((k + 1) log n)$。

*实现* #link("https://qoj.ac/submission/2959409")[QOJ 提交 2959409]。
