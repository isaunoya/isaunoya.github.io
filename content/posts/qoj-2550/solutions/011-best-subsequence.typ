参考 #context {
  if query(<qoj-2550-114>).len() > 0 {
    link(<qoj-2550-114>)[\#114]
  } else {
    [\#114]
  }
}。二分答案$x$，统计满足$2a_i <= x$的 small 数量，以及相邻 small 之间能补入 big 的间隙数。

将数组首尾相连，按$a_i$递增插入位置，用有序集合维护循环前驱、后继。位置$i$在$x=2a_i$时成为 small，给自身加一。若插入前集合非空，设相邻位置为$u,v$，则$i$是间隙内的最小值，能作为 big 补入的条件为

$ a_i + max(a_u,a_v) <= x < 2a_i. $

将这段贡献挂在右端点$v$上，在两个阈值处分别加一、减一，空区间忽略。所有事件按阈值排序，用可持久化线段树维护位置权值，处理完同一阈值后保存版本。

询问$[l,r)$时，取阈值不超过$x$的最后一个版本，用 ST 表和二分找到首末 small $p,q$；不存在则不可行。先查询$[p+1,r)$的权值和再加一：排除$p$前可能跨出左边界的间隙，只补回$p$自身。再取$[l,p)$和$[q+1,r)$的最小值$m$，若$m+max(a_p,a_q) <= x$，则再加一，补上询问首尾的间隙。空区间最小值取$+infinity$，最终判断总数是否不少于$k$。

事件共$O(n)$个，每次判定用时$O(log n)$。设值域上界为$V$，总时间复杂度为$O(n log n + Q log V log n)$，空间复杂度为$O(n log n)$。

*实现* #link("https://qoj.ac/submission/2960713")[QOJ 提交 2960713]。
