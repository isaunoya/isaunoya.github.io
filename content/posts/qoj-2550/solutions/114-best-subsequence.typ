不妨钦定最小值被选：用它替换前一个选中的元素，相邻和都不会增大。将数组循环移位，把最小值放到第一位。

二分答案$X$，称满足$2w_i <= X$的元素为 small，其余为 big。两个 small 可以相邻，两个 big 不能相邻。

small 可以全选。补入一个未选的 small，若与相邻的 big 冲突，就用它替换这个 big；big 原本两侧都是 small，因此替换后仍然合法，数量也不减少。故为了选 big 而放弃 small 不会更优。

若 small 已有$k$个则可行，否则考虑相邻 small 之间的间隙。每个间隙至多补入一个 big，取其中的最小值$m$，两端值记为$u,v$，满足$m+max(u,v) <= X$即可补入。统计可选总数，判断是否达到$k$。

从第一位扫描一圈，维护前一个 small 和间隙最小值，最后再访问第一位以结算首尾间隙。没有 small 时不可行。总时间复杂度为$O(n log V)$，其中$V=max_i w_i$，空间复杂度为$O(n)$。

*实现* #link("https://qoj.ac/submission/2959650")[QOJ 提交 2959650]。
