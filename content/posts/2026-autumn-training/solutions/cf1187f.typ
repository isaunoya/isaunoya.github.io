#import "/.typst-blog/typst/blog.typ" as blog

本题是在“连续段数的期望”这一经典模型上，把一次矩改成了二次矩。一次矩只需每条边的边缘概率；二次矩还要考虑两条边同时发生的概率。由于相关性只出现在相邻边之间，原题仍可在线性时间内解决。进一步将平方改为 $k$ 次方，就得到后文的连续段容斥与 DP。

=== 原题：指示变量与二次矩

==== 把段数拆成边的贡献

令 $Y_i = [X_i != X_(i+1)]$，其中 $0 <= i < n - 1$，方括号表示命题成立时为 $1$，否则为 $0$。每次相邻元素不同，都会多出一个连续段，因此

$ B = 1 + sum_(i=0)^(n-2) Y_i. $

记 $p_i = E[Y_i]$。一次矩可以直接使用期望的线性性：

$ E[B] = 1 + sum_(i=0)^(n-2) p_i. $

计算二次矩时展开平方。由 $Y_i^2 = Y_i$，有

$ E[B^2] = 1 + 3 sum_(i=0)^(n-2) p_i + 2 sum_(0 <= i < j < n-1) E[Y_i Y_j]. $

这里不能直接把所有 $E[Y_i Y_j]$ 换成 $p_i p_j$，也不能把 $E[B^2]$ 换成 $E[B]^2$。

==== 只有相邻边需要修正

$Y_i$ 只依赖 $X_i, X_(i+1)$。若 $j >= i + 2$，则 $Y_i$ 与 $Y_j$ 依赖的随机变量集合不相交，由原变量相互独立可得

$ E[Y_i Y_j] = p_i p_j. $

相邻的 $Y_i, Y_(i+1)$ 共用 $X_(i+1)$，则需要单独计算。

记相邻两个、三个变量全部相等的概率分别为

$ a_i = (max(0, min(R_i, R_(i+1)) - max(L_i, L_(i+1)))) / (d_i d_(i+1)), $

$ t_i = (max(0, min(R_i, R_(i+1), R_(i+2)) - max(L_i, L_(i+1), L_(i+2)))) / (d_i d_(i+1) d_(i+2)). $

分子是公共取值的数量；由于各位置独立均匀取值，分母是取值方案总数。于是 $p_i = 1 - a_i$，再对相等事件容斥：

$ E[Y_i Y_(i+1)] = 1 - a_i - a_(i+1) + t_i. $

从而相邻边相对于独立情况的修正量为

$ E[Y_i Y_(i+1)] - p_i p_(i+1) = t_i - a_i a_(i+1). $

记 $mu = 1 + sum_(i=0)^(n-2) p_i$，将所有非相邻对的贡献合并进 $mu^2$，便得到

$ E[B^2] = mu^2 + sum_(i=0)^(n-2) p_i (1 - p_i) + 2 sum_(i=0)^(n-3) (t_i - a_i a_(i+1)). $

第二项修正同一条边的平方，第三项修正相邻两条边的乘积。这也就是“均值的平方加方差”的展开形式。

实现时预处理各 $d_i$ 的逆元，扫描相邻二元组与三元组即可。若将模数视为常数，时间复杂度为 $O(n)$，保存区间时空间复杂度为 $O(n)$；若显式计入快速幂求逆的代价，时间复杂度为 $O(n log P)$。

当 $n = 1$ 时所有求和均为空，答案为 $1$。第一个样例中 $p_0 = 1/2$、$p_1 = 2/3$，且 $t_0 = a_0 a_1 = 1/6$，代入得 $E[B^2] = 31/6$，对应模意义下的 $166666673$。

=== 加强：求任意 k 次矩

给定非负整数 $k$，若改为求 $E[B^k]$，直接展开会出现 $k$ 个下标，无法逐项枚举。但这些项具有两个结构：指示变量的重复幂可以合并，而不同下标组成的集合可以拆成若干极大连续段。下面分别利用这两个结构。

==== 用哨兵消去常数项

在末尾添加恒为 $0$ 的 $X_n$，即区间 $[0, 1)$。原题所有取值均为正，因此新增的最后一条边必然满足 $Y_(n-1) = 1$，于是

$ B = sum_(i=0)^(n-1) Y_i. $

此后记边数为 $m$，即有 $m+1$ 个变量、$m$ 条边；对原题添加哨兵后 $m=n$。通用函数计算的是这些边上的变化次数之和的矩，调用方通过哨兵将其转换为原题的段数。

==== 先求组合矩

对非负整数 $B$，有恒等式

$ B^k = sum_(j=0)^k S(k,j) j! binom(B,j), $

其中 $S(k,j)$ 是第二类 Stirling 数，表示将 $k$ 个有标号元素划分成 $j$ 个非空集合的方案数，满足

$ S(k,j) = S(k-1,j-1) + j S(k-1,j). $

初值为 $S(0,0)=1$；当 $k>0$ 时 $S(k,0)=0$，当 $j>0$ 时 $S(0,j)=0$，越界状态均为 $0$。

这个恒等式可以直接从 $(sum_i Y_i)^k$ 的展开理解：将 $k$ 个乘数按所选下标分组，若共使用 $j$ 个不同下标，则分组有 $S(k,j)$ 种，再把这 $j$ 组对应到选出的 $j$ 个下标，有 $j!$ 种。由于 $Y_i$ 只有 $0,1$ 两种取值，同一下标重复出现不会改变乘积。

因此只需要计算

$ A_j = E[binom(B,j)] = sum_(S subset.eq {0, dots, m-1}, abs(S)=j) E[product_(i in S) Y_i]. $

最后还原

$ E[B^k] = sum_(j=0)^q S(k,j) j! A_j, quad q = min(k,m). $

$A_j$ 是组合矩；乘上 $j!$ 才是下降阶乘矩。代码中的 `factorial_moments` 返回的是 $A_j$，阶乘在最后求和时乘入。

==== 不同连续段之间可以相乘

将选中的边集合 $S$ 拆成极大连续段。边段 $[l,r)$ 使用的原变量是 $X_l, dots, X_r$。两个不同的极大连续段之间至少有一条未选边，所以它们使用的原变量集合不相交，事件相互独立。

例如，选中 $Y_0,Y_1,Y_3$ 时，前两条边使用 $X_0,X_1,X_2$，后一条边使用 $X_3,X_4$。因此

$ E[Y_0 Y_1 Y_3] = E[Y_0 Y_1] E[Y_3]. $

只需预处理每个连续边段内部的联合概率，就能将整个集合的贡献写成各段概率的乘积。由于总共只选至多 $q$ 条边，需要的连续段长度也不超过 $q$。

=== 连续段概率：按容斥的最后一个连通块转移

定义

$ w_(l,t) = E[product_(i=l)^(l+t-1) Y_i], quad w_(l,0) = 1. $

它表示边段 $[l,l+t)$ 内每一对相邻变量都不同的概率。

再定义变量区间 $[s,r)$ 内全部相等的概率

$ c(s,r) = (max(0, min_(s <= i < r) R_i - max_(s <= i < r) L_i)) / (product_(i=s)^(r-1) d_i). $

这里 $s<r$，特别地 $c(s,s+1)=1$。固定右端点后向左扫描，可以同时维护最大左端点、最小右端点与逆元乘积，在常数时间内更新 $c(s,r)$。

令 $Z_i = [X_i = X_(i+1)]$，则 $Y_i = 1-Z_i$。对边段 $[l,r-1)$ 容斥：

$ w_(l,r-l-1) = sum_(T subset.eq {l, dots, r-2}) (-1)^(abs(T)) E[product_(i in T) Z_i]. $

容斥集合 $T$ 中的连续边会将对应变量连成等值连通块。按包含最后一个变量 $X_(r-1)$ 的连通块分类，设这个块的变量区间为 $[s,r)$。

- 块内 $r-s-1$ 条边全部选入 $T$，贡献符号 $(-1)^(r-s-1)$ 和概率 $c(s,r)$。
- 若 $s>l$，分隔边 $s-1$ 不选入 $T$；左侧可自由选择的边为 $[l,s-1)$，容斥和正好是 $w_(l,s-l-1)$。
- 若 $s=l$，没有左侧部分，其贡献为 $1$。

因此，定义

$ h(l,s) = cases(1 & "若" s=l, w_(l,s-l-1) & "若" s>l), $

则对于 $t>=1$、$r=l+t+1$，有

$ w_(l,t) = sum_(s=l)^(r-1) (-1)^(r-s-1) c(s,r) h(l,s). $

这里的“连通块”由*选入容斥集合的相等边*形成。分隔边没有选入 $T$，只表示这一容斥项未对它施加相等约束，并不要求实际取值不相等。这个区别保证了转移没有额外限制。

例如 $t=2$ 时，上式展开就是

$ w_(l,2) = 1 - c(l,l+2) - c(l+1,l+3) + c(l,l+3), $

与原题对相邻两条边的容斥一致。

对每个起点按长度递增计算 $w_(l,t)$，每个状态枚举最后一个等值块的起点，时间复杂度为 $O(m q^2)$。

=== 组合矩 DP：枚举最后一个选中段

令

$ D_(i,j) = sum_(S subset.eq {0, dots, i-1}, abs(S)=j) E[product_(x in S) Y_x]. $

即仅在边前缀 $[0,i)$ 中选出 $j$ 条边的总贡献。初始状态为 $D_(0,0)=1$，其余状态为 $0$；空集合的乘积规定为 $1$。

若不选最后一条边 $i-1$，贡献为 $D_(i-1,j)$。否则，设最后一个极大连续段为 $[i-t,i)$，长度为 $t$，则该段贡献 $w_(i-t,t)$。

当 $i>t$ 时，分隔边 $i-t-1$ 必须不选，前面的边只能从 $[0,i-t-1)$ 中选取，贡献为 $D_(i-t-1,j-t)$。当 $i=t$ 时，最后一段从 $0$ 开始，前缀为空，必须有 $j=t$。

记

$ g(i,j,t) = cases([j=t] & "若" i=t, D_(i-t-1,j-t) & "若" i>t), $

得到转移

$ D_(i,j) = D_(i-1,j) + sum_(t=1)^(min(i,j)) w_(i-t,t) g(i,j,t). $

这里前缀下标必须是 $i-t-1$。若使用 $i-t$，就可能继续选中分隔边，使两个段相连；此时既重复拆分同一集合，也无法再将两段的概率相乘。

最终 $A_j = D_(m,j)$。代回 Stirling 数公式，即得 $E[B^k]$。特别地，$k=2$ 时只有 $S(2,1)=S(2,2)=1$，所以答案就是 $A_1+2A_2$，与逐对展开二次矩相同。

=== 复杂度与实现

令 $q=min(k,m)$，连续段概率预处理和组合矩 DP 各需要 $O(m q^2)$ 时间，Stirling 数使用一维倒序递推，需要 $O(k q)$ 时间。计入零次矩和没有边的情况，且将模数视为常数，总复杂度为

$ O(m(q+1)^2 + k(q+1) + 1), $

空间复杂度为 $O((m+1)(q+1))$。若显式计算求逆代价，时间中还需加上 $O((m+1) log P)$。当 $k=2$ 时，此实现仍为线性规模；当 $k$ 较大时，平方因子会限制可处理的数据规模。

以下实现沿用 #link("https://isaunoya.github.io/libra/")[noya 竞赛模板]。`expected_moment` 接受左闭右开的非空取值区间；`solve` 在输入处转换右端点并添加零哨兵，默认计算二次矩。原题各区间长度均小于 $P$，因此逆元存在。一般化调用也需满足每个区间长度在模 $P$ 下可逆。

#blog.code(lang: "cpp", label: "C++：k 次矩实现")[
#raw(read("cf1187f.cpp"), lang: "cpp", block: true)
]

本题可复用的结构是：将统计量拆成局部指示变量，按底层随机变量是否重叠判断独立性。二次矩只需修正相邻边；更高次矩则先按不同下标的数量转为组合矩，再利用连续段分解处理局部依赖。
